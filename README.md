# Цифровой ассистент Клуба Здоровья

Telegram-бот для персонального сопровождения участников Клуба Здоровья.

## Возможности

- 📋 Первичное анкетирование и оценка состояния здоровья
- 📊 Ежедневное отслеживание показателей
- 🍎 Учёт питания и калорий
- 📈 Статистика и отчёты (неделя/месяц)
- 💧 Напоминания о воде
- 🔄 Повторное тестирование (раз в месяц)
- 👨‍💼 Связь с администрацией
- 🔐 Роль администратора с доступом к статистике

## Установка

1. Клонируйте репозиторий
2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # для Linux/Mac
# или
venv\Scripts\activate  # для Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

**Примечание:** Проект использует библиотеку `aiogram 3.4.1` для работы с Telegram Bot API.

4. Скопируйте `.env.example` в `.env` и заполните:
```bash
cp .env.example .env
```

5. Заполните `.env` файл, указав:
   - `BOT_TOKEN` - токен вашего Telegram бота (получить у @BotFather)
   - `ADMIN_USER_IDS` - ID администраторов через запятую

6. Создайте папку для данных:
```bash
mkdir -p data uploads temp
```

7. Запустите бота:
```bash
python main.py
```

## Структура проекта

```
healthy_bot_AI/
├── main.py                 # Точка входа
├── config.py              # Конфигурация
├── database/
│   ├── __init__.py
│   ├── models.py          # Модели БД
│   └── db.py              # Подключение к БД
├── services/
│   ├── __init__.py
│   ├── onboarding.py      # Онбординг и анкетирование
│   ├── daily_scenarios.py # Ежедневные сценарии
│   ├── nutrition.py       # Работа с питанием
│   ├── statistics.py      # Статистика и отчёты
│   ├── admin.py           # Модуль администратора
│   └── retest.py          # Повторное тестирование
├── handlers/
│   ├── __init__.py
│   ├── commands.py        # Обработчики команд
│   ├── messages.py        # Обработчики сообщений
│   └── callbacks.py       # Обработчики callback'ов
├── utils/
│   ├── __init__.py
│   ├── calculations.py    # Расчёты (ИМТ, калории, БЖУ)
│   ├── templates.py       # Шаблоны сообщений
│   └── validators.py      # Валидация данных
├── data/                  # База данных
├── uploads/               # Загруженные файлы
└── temp/                  # Временные файлы
```

## Команды бота

- `/start` - Начать работу / повторить анкетирование
- `/menu` - Главное меню
- `/statistics` - Показать статистику
- `/report` - Сформировать отчёт
- `/contact_admin` - Связаться с администратором
- `/retest` - Пройти повторное тестирование

## Лицензия

Внутренний проект Клуба Здоровья
